# alv.js — Alvará Userscript (PATCH 1 + FASE 1)

- Arquivo ativo: `d:\PjePlus\alv.js` (não confundir com `alv.juser.js`, cópia antiga com PATCH 1 parcial e um segundo header UserScript colado no fim).
- PATCH 1: detecção de "depósito efetuado ... libere-se ao exequente" sem valor monetário → `creditoPorDepositoSemValor`, card com aviso `.pje-alvara-pendente`, export `window.PjeAlvara.{extrairReferenciaDeposito, REGEX}`. Correção interna: `trecho` do patch original terminava no id (banco nunca seria extraído) — estendido +60 chars após o match.
- FASE 1 (2026-08-31): novos tipos "Devolução à reclamada" e "Transferência para outro processo" (REGEX.devolucaoReclamada / transferenciaOutroProcesso), helpers (detectarPrimeiroPadrao, extrairValorGenerico, extrairIdDepositoRelacionado, extrairProcessoDestino, criarIdItem), itens automáticos em criarEstado (spread), botão "Adicionar tipo" (prompt) + remover card, `tipoConta` nos dados bancários, footer com `.pje-alvara-footer-actions`.
- Correção no patch FASE 1: regexes `#id\s*:` exigiam "#" literal, mas o texto de teste 1 usa "id:9528985" sem "#" → trocado para `#?\s*id`. Com isso T1 bate com o resultado esperado do patch.
- Bug do botão (corrigido): `iniciar()` retornava cedo se pathname não fosse /detalhe no document-idle (SPA Angular muda rota depois) → botão nunca injetado. Fix: guard pronto (readyState/body) + observer verifica pathname a cada mutação.
- Testes validados em node com DOM stubado (T1..T6): ver histórico de terminal 2026-08-31.
- Fase 2 (futuro): integração SISCONDJ — não implementar fetch/API na fase 1.
- v0.2.1 + extrator incorporado (2026-08-31): obterTextoDecisao() agora é async e usa window.pjeExtrair SE existir; senão extrator local (xtrairDocumentoAtualLocal): fontes na ordem HTML do visualizador → iframe acessível → object → embed → seleção do usuário, com polling 300ms/timeout 10s (aguardarTextoDocumentoAtual). analisarDecisao nunca aborta por visualizador; logs de texto/valores. Exports: obterTextoDecisao, extrairDocumentoAtualLocal, obterTextoDocumentoAtual. Pipeline completo validado em node com texto acentuado: flag=true, deposito #9528985 BB. Lembrete: regexes exigem acentos corretos ('depósito', 'crédito') — texto sem acento não casa.
- Reuso do extrator canonico (2026-08-31): alv.js agora tem @require https://raw.githubusercontent.com/silaspassosf/pjeplus/main/Script/core/extrair.js (mesmo padrao do hcalc.user.js com calc/BASE). extrair.js e IIFE que expoe window.pjeExtrair (DOM+PDF via pdfjsLib do PROPRIO viewer do PJe — nao precisa de @require extra) e window.pjeExtrairApi. O loader.js (pjetools.user.js) ja carrega extrair.js + registrar_debito.js em serie; debito.js so CONSUME pjeExtrair. alv.js: obterTextoDecisao prefere extrator externo; extrator local incorporado ficou como fallback se @require falhar. @require e baixado no save/update do TM (funciona offline depois). NAO confundir com alv.juser.js (copia velha).
- v0.3.0 overlay lateral (2026-08-31): overlay nao e mais dialog central escuro — painel fixo a direita (430px, height 100vh, fundo solido, sem scrim, fecha so pelo X). analisarDecisao SEM confirm/alert de abertura: sempre abre; criarEstado so monta itens dos tipos DETECTADOS (credito/creditoPorDepositoSemValor, inss, custas, honorariosAdv/Per, devolucao, transferencia); estado vazio = linha .pje-alvara-empty 'Nenhum tipo de verba detectado'. Footer: select data-novo-tipo + botao azul 'Adicionar Verba' (.pje-alvara-add #2563eb) centralizado ACIMA do 'Criar alvaras' (footer em coluna). escolherTipoManual/window.prompt removidos. Estado salvo NAO e mais recarregado automaticamente na abertura (sempre recria da decisao); adicoes manuais persistem so na sessao do overlay. Validado em node: prompt/confirm jamais chamados, itens condicionais corretos.
- v0.4.0 dados do processo (2026-08-31): API pje-comum-api igual hcalc-prep (fetch credentials include, XSRF-TOKEN do cookie, X-Grau-Instancia 1): GET /pje-comum-api/api/processos/id/{id} (numeroProcesso) + /partes (ATIVO/PASSIVO/TERCEIROS; parte = nome, documento, representantes[{nome, numeroOab, documento?}]). buscarDadosProcesso() roda A CADA analise (dados sempre corretos) e e salvo em estado.processo (localStorage junto do estado). Preenchimento automatico (aplicarPreenchimentoAutomatico, usado por criarEstado E criarItemManual via _dadosProcessoCache): credito c/ destino advogado/escritorio -> 1o advogado do ATIVO (Nome+CPF); destino 'Conta reclamante' -> 1a parte do ativo; devolucao -> 1a parte PASSIVO (procurador -> 1o advogado passivo); honorarios advocaticios -> SEMPRE 1o advogado do autor (da reclamada nunca automatico, so via Adicionar Verba). Perito: REGEX.peritoNome detecta nome no texto -> item.perito (honorarios periciais). API expoe buscarDadosProcesso.
- v0.4.0 peritos (2026-08-31): buscarDadosProcesso tambem busca GET /pje-comum-api/api/processos/id/{id}/peritos (aceita strings ou {nome|pessoa.nome, documento}); fallback: TERCEIROS de /partes com tipo/nome contendo 'perito' -> _shapePeritos. Cache: _dadosProcessoCache.peritos; salvo em estado.processo.peritos. Regra honorarios periciais (aplicarPreenchimentoAutomatico): verba detectada SEM nome na decisao -> perito = primeiro perito da API; nome extraido (REGEX.peritoNome) prevalece quando presente; limpeza de preposicoes finais (regex capturava 'Maria Contadora em'); verba NAO detectada -> campo nem existe. Vale tambem para item adicionado manualmente. Validado em node com mock /peritos e fallback TERCEIROS.
- v0.4.1 deteccao + logs (2026-08-31): (1) logs limpos — logDiagnostico virou no-op e logs de rotina removidos; ficam SO os logs de extracao em analisarDecisao: '[PjeAlvara] TEXTO EXTRAIDO DA DECISAO', 'PADROES RECONHECIDOS' (JSON com credito/creditoPorDepositoSemValor/creditoParcialDeposito/deposito/inss/custas/honorarios/peritoNome/devolucao/transferencia) e 'ITENS CRIADOS NO OVERLAY'; logAviso mantido so para falhas. (2) depositoCreditoSemValor agora aceita femininos e preposicoes: ao/à/para o/para a/em favor do|da + exequente/exequenta/reclamante/autora?/demandante ('libere-se à autora' funciona). (3) 'parte de seu credito' -> creditoParcialDeposito=true -> valor do credito vira 'Depósito integral atualizado' com valorFixo=true (input readonly data-fixo, sem mascara monetaria). (4) ids hex com colchetes: #id:[69f484f] aceito (regex \w+ com colchetes opcionais) + palavra-chave 'extrato' em extrairIdDepositoRelacionado; credito usa extrairIdDepositoRelacionado (cobre depósito efetuado/extrato/guia/parcela). Teste com a decisao real do usuario passou.
- v0.4.2 overlay compacto (2026-08-31): largura 430->560px (+30%); paddings/margins/fontes reduzidos em tudo (content 8/10, card 7/9 margin 8, labels 11px margin 3, inputs padding 4/7, footer 7/10 gap 5, header 7/12); alerta de 4 linhas virou 1 linha curta; add-row agora HORIZONTAL (select flex1 + botao Adicionar Verba lado a lado, ainda acima do Criar alvaras). Campos de ID/Banco do deposito REMOVIDOS do UI (credito/devolucao/transferencia) — id segue extraido e salvo no estado via obterDadosDoCard? NAO: sem os inputs, dados.deposito nao e mais reconstruido no atualizarEstadoPeloOverlay (so fica no estado inicial); usuario confirmou irrelevante. Ordem dos campos bancarios: Nome -> CPF/CNPJ -> Agencia -> Conta -> Banco -> Tipo de conta. dados-grid 2 colunas.
- v0.5.0 MODULARIZACAO (2026-08-31): alv.js monolitico (2846 lin) -> alv.user.js orquestrador (414 lin) + Script/alvara/{utils 85, extracao 674, dados_processo 274, estado 495, estilos 306, overlay 732, extracao_siscondj, minuta}. Namespace window.Alv (utils/log/const/extracao/dados/estado/estilos/overlay/minuta/siscondj); PjeAlvara vira fachada no orquestrador. @require dos modulos com ?v=1 (bumpar todos juntos). minuta.js = placeholder DEFINITIVO (contrato iniciarFluxoMinuta(estado)->{iniciado,motivo}); botao Criar alvaras chama Alv.minuta via overlay.js. extracao_siscondj.js scaffold com CONFIG (dominios/seletores/endpoints) e extrairDadosSiscondj guardado por dominio. Montagem: recorte por linhas do alv.js + heads/tails com aliases (const utils = Alv.utils etc). alv.js permanece como legado; guard INSTANCE_KEY compartilhado evita dupla execucao se os dois estiverem instalados. Validado node: fluxo completo via fallback local (extract->dados->estado->overlay->minuta). ATENCAO: extracao.js usa funcoes locais; novos pontos: Alv.log.diag e no-op.
- v0.6.0 SISCON automatico (2026-08-31): novo Script/alvara/siscon_consulta.js (porta do sis.js v1.3.0 sem UI): consulta dados bancarios TRT2 (aplicacoes1.trt2.jus.br/adv-dados-bancarios-consulta) via GM_xmlhttpRequest (fallback fetch) com ViewState JSF. Regras de rota: advogado/procurador -> /index depois /pf COM atencao informando segunda busca; propria parte (Conta reclamante/reclamada) -> CPF=/pf CNPJ=/consulta-pj (nunca /index); escritorio -> NPJ /consulta-pj (um dominio; sem CNPJ = erro instrutivo). Gatilhos: abrirOverlay->inicializar (verbas detectadas), troca de destinoTipo (change), Adicionar Verba, e botao Puxar do SISCON virou FALLBACK manual (reconsulta, nao alert). Memo por card|modo|doc. Found: preenche inputs vazios (banco/agencia/conta/tipo/nome/doc se vazio) + merge no estado via _salvarDadosNoEstado (NUNCA reconstruir itens) + 'link dos dados' = detailUrl (pagina final da extracao). Empty: status claro + link busca pre-preenchida (buildSearchLink). Error: status + botao SISCON. alv.user.js: @grant GM_xmlhttpRequest + @connect aplicacoes1.trt2.jus.br + @require siscon_consulta.js + PjeAlvara.siscon. Validado node com mock GM/DOMParser: rota /index ok, rota /pf com atencao ok, cnpj vazio ok.
