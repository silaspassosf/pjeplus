- Resiliencia global API 5xx (2026-08-31): novo Play/pjeplay/api_resiliencia.py envelopa requests.Session.request UMA vez (chamado em pjeplay.iniciar(), que pw.py sempre roda antes de qualquer import de negocio). Em GET 5xx de *.jus.br com driver vivo: driver.refresh() (F5), espera readyState (~15s), ressincroniza cookies do driver na Session, retry x2 com pausa 3s. Driver ativo registrado em launcher.criar_driver (funil unico) via api_resiliencia.registrar_driver. Vale para QUALQUER fluxo (P2B/PEC/Mandado/SISB/Peticao/bianca) pois todos usam requests.Session via session_from_driver. Validado: hook ativo (qualname instalar.<locals>.request). So GET — POST nao e re-disparado (risco de duplicidade).
- Log 165950: 1000257/1001125 NAO falharam no modelo (modelo inserido + snackbar ok); falharam no FIM: "Botao Sim nao encontrado" (dialog de confirmacao do movimento atrasado) e [SALVAR_FINAL] ElementHandle.click timeout 30s "waiting for element to be stable". 1001986/1000269 normais.
- Causa raiz dependencia de janela visivel/ativa: gate de actionability do clique nativo Playwright usa rAF; aba/janela inativa no Firefox nao dispara rAF -> clique nunca "estabiliza" -> timeout 30s. Angular com timers throttled ao fundo tambem atrasa dialogs (Botao Sim).
- FIX (2026-08-31): JS-first em todo clique do motor — Play/pjeplay/element.py PWElement.click() agora evaluate("el=>el.click()") com fallback nativo force=True timeout 10s; Play/pjeplay/nativo.py _clicar() agora wait_for_selector(attached) + evaluate JS click (mesmo padrao Selenium execute_script click), _CLIQUE_JS como fallback. PREFS_BASE ja tinha anti-throttle de timers, mas rAF em aba oculta nao tem pref — por isso o fix foi no clique, nao nos prefs.
- CORRECAO do usuario (2026-08-31): snackbar "Modelo de documento inserido com sucesso" NAO e prova — aparece tambem salvando com editor VAZIO. 1000257/1001125 no log 165950 tiveram modelo inserido APARENTE mas editor vazio (minuta salva sem modelo). Padrao correto de verificacao: gigs-plugin.js aaDespacho verificarSeExisteTextoNoEditor (linha ~10839) — editor = div[class*="area-conteudo"][contenteditable="true"], prova = innerText sem espacos > 1 char OU figure presente (null = editor ainda nao renderizou).
- FIX: atos/judicial_fluxo.py bloco "Inserir modelo" reescrito em loop de 3 tentativas: nodo-filtrado -> dialog -> botao inserir (JS click) -> fecha dialog -> verifica js_editor_tem_conteudo no editor (10x1s). Se vazio apos 3 tentativas: return False,False ANTES do Salvar (nao salva minuta vazia; com o fix da tupla em p2b_documentos o processo falha a regra e nao e marcado como executado).
- SALVAR_FINAL timeout era artefato do gate de estabilidade (rAF), ja resolvido pelo JS-first click; o proprio usuario confirmou que o passo sempre funcionou.
- Refinamento final (2026-08-31, pedido do usuario: SEM retry, sequencia perfeita): padrao deterministico extraido de gigs-plugin.js inserirModeloNoDocumento (linha 10775): observer dispara com PJE-DIALOGO-VISUALIZAR-MODELO no DOM -> sleep(500) ANTES de clicar Inserir (guarda anti-corrida: preview/teor carregando) -> clique em button[aria-label="Inserir modelo de documento"] (aria-label estavel, NAO o caminho estrutural fragil pje-dialogo-visualizar-modelo > div > ...). atos/judicial_fluxo.py reescrito: nodo-filtrado (JS) -> aguardar dialog aparecer (8s) -> time.sleep(0.5) -> wait_for_clickable aria-label (8s, fallback CSS 3s) -> JS click -> snackbar informativa -> dialog sumir (4s) -> verificacao de conteudo no editor (10x1s, espera de carga, nao retry). Falha -> return False,False antes do Salvar. Retry de reinsercao REMOVIDO.
