// BOOKMARKLET SISCON - Extrair Saldos Automaticamente
// Copie a linha abaixo e salve como bookmark no navegador:

javascript:(function(){if(!window.location.href.includes('alvaraeletronico.trt2.jus.br')){alert('ERRO: Este bookmarklet só funciona no Alvará Eletrônico!');return}const tbl=document.querySelector('#table_contas');if(!tbl){alert('ERRO: Execute uma busca primeiro!');return}const proc=document.querySelector('#numeroProcesso')?.value||'Não identificado';const linhas=document.querySelectorAll('tr[id^="linhaConta_"]');let dados={processo:proc,data:new Date().toLocaleString('pt-BR'),contas:[],total:0};let texto=`=== DADOS SISCON ===\nProcesso: ${proc}\nData: ${dados.data}\n\nCONTAS COM SALDO:\n`;for(let linha of linhas){try{const conta=linha.querySelector('td:nth-child(2)')?.textContent.trim();const saldo=linha.querySelector('td[id*="saldo_corrigido_conta_"]')?.textContent.trim();const valor=parseFloat(saldo?.match(/([0-9.,]+)/)?.[1]?.replace(/\./g,'').replace(',','.')||'0');if(valor>0){const expansao=linha.querySelector('img[src*="soma-ico.png"]');if(expansao)expansao.click();dados.contas.push({conta,valor});dados.total+=valor;texto+=`Conta: ${conta} - Disponível: R$ ${valor.toFixed(2).replace('.',',')}\n`}}catch(e){}}setTimeout(()=>{texto+=`\nTOTAL GERAL: R$ ${dados.total.toFixed(2).replace('.',',')}\n\nGerado automaticamente pelo Bookmarklet SISCON`;if(navigator.clipboard)navigator.clipboard.writeText(texto).then(()=>alert(`Extração concluída!\n\nContas encontradas: ${dados.contas.length}\nValor total: R$ ${dados.total.toFixed(2).replace('.',',')}\n\nDados copiados para área de transferência!`));else{const ta=document.createElement('textarea');ta.value=texto;ta.style.position='fixed';ta.style.opacity='0';document.body.appendChild(ta);ta.select();document.execCommand('copy');document.body.removeChild(ta);alert(`Extração concluída!\n\nContas encontradas: ${dados.contas.length}\nValor total: R$ ${dados.total.toFixed(2).replace('.',',')}\n\nDados copiados para área de transferência!`)}},2000)})();
